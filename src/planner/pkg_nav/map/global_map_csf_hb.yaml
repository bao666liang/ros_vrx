image: global_map_csf_hb.png #被描述的图片资源路径，可以是绝对路径也可以是相对路径。
resolution: 1 # 图片分片率(单位: m/像素)。
origin: [0, 0, 0] #地图中左下像素的二维姿势，为（x，y，偏航），偏航为逆时针旋转（偏航= 0表示无旋转）。
occupied_thresh: 0.4 #占用概率大于此阈值的像素被视为完全占用。
free_thresh: 0.1 # 占用率小于此阈值的像素被视为完全空闲。
negate: 1 #是否应该颠倒白色/黑色自由/占用的语义。

